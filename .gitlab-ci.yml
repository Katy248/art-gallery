workflow:
  rules:
    - if: $CI_COMMIT_BRANCH

deploy-pages:
  script:
    - ls
  pages: true  # specifies that this is a Pages job
  artifacts:
    paths:
      - .
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
