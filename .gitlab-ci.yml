workflow:
  rules:
    - if: $CI_COMMIT_BRANCH

deploy-pages:
  script:
    - mkdir -p public/
    - cp -r src/ public/
  pages: true  # specifies that this is a Pages job
  artifacts:
    paths:
      - public/
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
